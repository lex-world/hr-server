module.exports = {
  attendance: async () => {
    // const { _id, entryTime, exitTime } = req.body;

    try {
      /** @dev check if user already logged in on the same day */
    } catch (error) {
      console.log(error);
    }
  },
};
